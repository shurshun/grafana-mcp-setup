{{ .Chart.Name }} is serving {{ include "grafana-mcp-setup.basePath" . }} on {{ .Values.appPublicURL | trimSuffix "/" }}.

Grafana API mode is {{ .Values.grafana.apiMode }}.
{{- if eq .Values.grafana.apiMode "iam" }}
Enable Grafana feature flags kubernetesServiceAccountsApi and kubernetesServiceAccountTokensApi
before starting this issuer. The configured namespace is {{ .Values.grafana.namespace }}.
{{- end }}

Authentication mode is {{ .Values.auth.mode }}.
{{- if eq .Values.auth.mode "native" }}
Native mode performs the OIDC login in this application. It can run behind the
ordinary Ingress or HTTPRoute above; do not enable securityPolicy in this mode.
{{- end }}

{{- if .Values.grafana.serviceAccountToken.create }}
This release creates GrafanaServiceAccount {{ include "grafana-mcp-setup.serviceAccountTokenName" . }}.
Grafana Operator and the grafana.integreatly.org/v1beta1 CRD must already be
installed. The GrafanaServiceAccount and its token Secret must share the
Grafana instance namespace with this Helm release. The operator-managed
resource keeps spec.instanceName and spec.name immutable. Its token omits an
expiry, so the operator requests a non-expiring issuer token subject to
Grafana policy. Use the operator's token rotation procedure, then restart
this Deployment after the credential Secret changes.
{{- end }}

Before anyone can use it:

1. Grafana needs a service account with the Admin role, and its token in
   {{ if .Values.grafana.serviceAccountToken.create }}the operator-managed secret {{ include "grafana-mcp-setup.adminTokenSecret" . }}{{ else if .Values.grafana.existingSecret }}the secret {{ .Values.grafana.existingSecret }}{{ else }}grafana.adminToken{{ end }}.
   Admin is not optional — creating service accounts needs it.

2. The identity provider needs a client {{ .Values.oidc.clientID | quote }} whose
   redirect URI is {{ .Values.appPublicURL | trimSuffix "/" }}{{ include "grafana-mcp-setup.basePath" . }}/oauth2/callback.

{{- if not .Values.oidc.requiredGroups }}

3. allowAllAuthenticatedUsers is explicitly enabled, so anyone who can sign in to the identity
   provider can issue themselves a Grafana token — including people Grafana
   itself would refuse, because the login in front of this service runs against
   the provider and not against Grafana. Set it unless that is what you meant.
{{- else }}

3. The provider must emit the groups claim, and holding any of
   {{ join ", " .Values.oidc.requiredGroups }} is what lets someone issue a token.
{{- end }}

{{- if not (or .Values.ingress.enabled .Values.httpRoute.enabled) }}

Neither ingress nor httpRoute is enabled, so nothing routes to the service yet.
{{- end }}
